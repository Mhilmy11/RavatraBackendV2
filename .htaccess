RewriteEngine On

# Jika file atau folder benar-benar ada, jangan di-rewrite
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Semua request diarahkan ke index.php
RewriteRule ^ index.php [L,QSA]